# Data

The original Energia dataset is proprietary and is not included.

Expected local private path:

```text
data/private/MarketData_2022-2026.parquet
```

The `data/sample/` folder contains synthetic data for structure and unit tests only. It must not be used to claim the submitted project results.
